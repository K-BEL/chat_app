// Avatar configuration
// Set USE_SINGLE_MODEL to true to use a single local model instead of Ready Player Me
export const AVATAR_CONFIG = {
  // Use a single local model instead of Ready Player Me avatars
  USE_SINGLE_MODEL: true,
  
  // Path to your local GLB model file (relative to public folder)
  // Place your model in the public/models/ folder
  MODEL_PATH: '/models/avatar.glb',
  
  // Model name (for display purposes)
  MODEL_NAME: 'My Avatar'
}

